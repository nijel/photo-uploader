Photo uploader installation
===========================

It uses standard distutils, so:
python setup.py build
sudo python setup.py install

You need PycURL[1] installed to run and install this program.

[1]: http://pycurl.sourceforge.net/
